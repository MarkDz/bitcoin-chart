{
  "homepage": "https://markdz.github.io/bitcoin-chart/",
  "name": "bitcoin-chart",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d dist -b master"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "sass": "^1.58.1",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "d3": "^7.8.2"
  }
}
